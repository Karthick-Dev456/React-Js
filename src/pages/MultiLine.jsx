const MultiLine = () => {
    return (
        <>
            <h2>Heading for Multiple Elements</h2>
            <p>This is a paragraph under the heading. By using a React Fragment, we can return multiple HTML elements from a single component.</p>
            <button>Click Me</button>
        </>
    );
};

export default MultiLine;
